<div class="container">
    <div class="row">
        <div class="col-lg-6">

            <h1 class="mb-3">Användare med tillgång</h1>

            {{#if usersWithAccess.length}}

                <ul class="list-group">
                    {{#each usersWithAccess}}
                        <li class="list-group-item d-flex justify-content-between align-items-start">
                            <p class="h4">{{this.firstName}} {{this.lastName}}</p>
                        {{#if ../userIsOwner}}
                            <a class="btn btn-primary" href="./removeUser/{{this.id}}"><i class="bi bi-x-circle me-2"></i>Ta bort</a>
                        {{/if}}
                        </li>
                    {{/each}}
                </ul>

            {{else}}
                <p>Inga användare har tillgång till projektet</p>
            {{/if}}

        </div>
    </div>
</div>  